---------------------------------------------------Iterator/Iterable-------------------------------------------------------------------
1.1)Написать внутри своего ArrayList два внутренних класа MyIterator and MyReverseIterator реализующих интерфейс Iterator для 
итерации в обоих направлениях. Также реализовать метод remove() из интерфейса Iterator;
1.2)Написать внутри своего ArrayList клас StrangeIterator, который будет итерировать по порядку через заданое константное 
количество елементов.


2)Создать клас MyTinyArrayList, который не будет наследовать интерфейс List. У этого класа будут только методы add(E e), 
remove(int index), size(), isEmpty(). С помощью интерфейса Iterable сделать возможным итерировать используя анонимный клас.



----------------------------------------------Comparator/Comparable------------------------------------------------------

3.1)Написать через интерфейс Comparable сортровку студентов(name,surname,age,averageMark) по имени в алфавитном порядке.
3.2)Написать через интерфейс Comparable сортровку работников(name,surname,secondName,salary) по зарплате(от большой к маленькой).
3.3)Написать через интерфейс Comparator несколько реализаций компаратора для сортировки студентов по имени в алфавитном порядке и
наоборот, по фамилии в алфавиному пордке и наоборот, по возрасту(от большей и от меньшей). И организовать выбор вариантов через
перечисления и внутриние класы(реализации Comparator).
3.4)Написать через интерфейс Comparator несколько реализаций компаратора для сортировки работников по имени в алфавитном порядке и
наоборот, по зарплате (от большей и от меньшей). И организовать выбор вариантов через перечисления и анонимные класы(реализации 
Comparator).